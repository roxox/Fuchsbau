<div>
    <span style="font-size: small; color: #61625d">
    <!-- Three columns of text below the carousel -->
    <div class="container">
        {#<h2>Projekt</h2>#}
        {#<hr>#}
        <div class="row">
            {# RECHTE SEITE#}

            <div class="col-lg-12">

                {# Projektinfo#}
                {% include "Projekte/project_info.html.twig" %}

                {# Projektbeteiligte#}
                {% include "Projekte/project_beteiligte.html.twig" %}
                <br>
                {# Kostenhinweis #}
                {#<div class="panel panel-warning">#}
                    {#<div class="panel-heading"><strong>Info bevor wir über die Kosten reden:</strong></div>#}
                    {#<div class="panel-body"><center><strong>Info bevor wir über die Kosten reden:</strong> Sofern tatsächliche Kosten#}
                            {#angegeben wurden, werden diese zur Kalkulation verwendet. Sollten noch keine tatsächlichen#}
                            {#Kosten, sondern lediglich geplante Kosten angegeben sein, werden diese in der Kalkulation#}
                            {#berücksichtigt. Im Falle, dass das gar keine Kosten angegeben sind, werden auch keien Kosten#}
                            {#zur Kalkulation rangeführt!#}
                        {#</center></div>#}
                {#</div>#}

                <div class="alert alert-warning fade in">
                    {#<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>#}
                    <center><strong>Info bevor wir über die Kosten reden:</strong> Sofern tatsächliche Kosten
                        angegeben wurden, werden diese zur Kalkulation verwendet. Sollten noch keine tatsächlichen
                        Kosten, sondern lediglich geplante Kosten angegeben sein, werden diese in der Kalkulation
                        berücksichtigt. Im Falle, dass das gar keine Kosten angegeben sind, werden auch keien Kosten
                        zur Kalkulation rangeführt!
                    </center>
                </div>

                {# Grundstück #}
                {% include "Projekte/project_grundstueck.html.twig" %}

                {# Haus #}
                {% include "Projekte/project_haus.html.twig" %}

                {# Nebenkosten #}
                {% include "Projekte/project_nebenkosten.html.twig" %}

                {# Gutschriften #}
                {% include "Projekte/project_gutschriften.html.twig" %}

                {# Außenalnagen #}
                {% include "Projekte/project_aussenanlagen.html.twig" %}

                {# Inneneinrichtung #}
                {% include "Projekte/project_inneneinrichtung.html.twig" %}
            </div>
        </div>
    </div>
    </span>
</div>