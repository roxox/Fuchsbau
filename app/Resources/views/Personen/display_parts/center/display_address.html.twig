<h4>Adressen</h4>
<div class="text">
    <p>Eine Adresse hast du mit Sicherheit schon, eine neue wird bald hinzukommen, hier kannst du
        deine
        aktuellen Adressen (egal ob privat, geschäftlich hinterlegen.</p><br>
</div>
<table class="table table-condensed">
    {% if person.getAdressen() != null %}
        {% for adresse in adressen %}
            <div align="right">
                <tr>
                    <td style="width: 60%;">
                        <p>{{ adresse.getStrasse() }} {{ adresse.getHausnummer() }}<br>
                            {{ adresse.getPostleitzahl() }} {{ adresse.getOrt() }}</p>
                        <p style="font-size: x-small">{{ adresse.getPrivatGeschaeft().getName() }}</p>
                    </td>
                    <td style="font-size: small; width: 40%" align="right">

                        <!-- Button Edit -->
                        <button type="button" class="btn btn-linkfx btn-xs" aria-label="Left Align"
                                href="{{ path('edit_address_by_current_person', {'addressId': adresse.getId()}) }}"
                                data-toggle="modal"
                                data-target="#editAddressPopup">
                            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Bearbeiten
                        </button>

                        <!-- Button Delete -->
                        <a class="btn btn-linkfxred btn-xs"
                           href="{{ path('remove_address_by_current_person', {'addressId': adresse.getId()}) }}">
                            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Löschen</a>

                    </td>
                </tr>
            </div>
        {% endfor %}
    {% else %}
        <tr>
            <td colspan="9" align="center">
                Noch wurde keine Adresse angelegt.
            </td>
        </tr>
    {% endif %}
    <tr>
        <td colspan="3"></td>
    </tr>
    <tr style="border: hidden">
        <td colspan="3" align="right">

            <!-- Button New -->
            <a class="btn btn-link-fx-brightblue btn-xs"
               href="{{ path('add_address_by_current_person') }}" data-toggle="modal"
               data-target="#addAddressPopup"
               role="button">
                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Adresse Hinzufügen
            </a>

        </td>
    </tr>
</table>
<div>
    <!-- Popups -->
    {% include "Personen/display_parts/center/popups/address/popup_address_new.html.twig" %}
    {% include "Personen/display_parts/center/popups/address/popup_address_edit.html.twig" %}
</div>